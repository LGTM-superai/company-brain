# updateGithub

Use this tool to update GitHub issues, PRs, labels, comments, or branches.

Before updating:
- identify exact repository and object
- summarize current state
- summarize intended change
- require approval for writes

After updating:
- return changed object URL
- return audit payload for updateSlack when relevant
