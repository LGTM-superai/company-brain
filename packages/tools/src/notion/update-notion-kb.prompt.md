# updateNotionKB

Use this tool to update Notion knowledge base pages.

Before updating:
- identify the exact page
- summarize the intended edit
- preserve existing page structure
- require approval for material policy or client-facing changes

After updating:
- return changed fields
- return source page URL or ID
- return audit message for updateSlack
